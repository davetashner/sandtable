node_modules
dist
coverage
poc
.claude
.beads
.agents
.codex
content/shared/media/**/*.json
package-lock.json

# generated by `npm run schema` (JSON.stringify output; keep byte-stable)
schema/

# Beat front matter: Prettier does not touch the prose (proseWrap defaults to
# preserve) and explodes the YAML flow sequences in `links:` into block form,
# which is longer and harder to scan than the line an author wrote. Nothing is
# gained and legibility is lost, so the beats format themselves.
content/eras/**/beats/*.md
