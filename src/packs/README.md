# src/packs

Scenario-pack loading and validation bindings: the TypeScript types and JSON
Schema for packs (`sand-a55.7`), the shared registries (`sand-a55.18`) and the
lazy loader (`sand-shn.1`). Content itself lives under `content/`, never here.
