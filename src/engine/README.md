# src/engine

Era-agnostic runtime: the timeline clock (`sand-a55.8`), the map and its
data layers (`sand-a55.9`, `sand-a55.11`), branches (`sand-a55.13`) and the
focus/zoom-in mechanism (`sand-a55.14`). Nothing in here knows about 1914;
it renders whatever scenario pack it is given.
